/// RAMPCore — service core of RAMP (paths, config, generators, installer, supervisor).
///
/// Dependencies are restricted to Apple system frameworks
/// (Foundation, CryptoKit, Darwin, OSLog) to keep the package auditable.
public enum RAMPCore {
    /// Version of the RAMPCore library.
    public static let version = "0.1.0"
}
