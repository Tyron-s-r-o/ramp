# RAMP changelog

Sections are copied verbatim into the Sparkle release notes (`app/scripts/release-app.sh` takes the
`## <version>` block) and the GitHub release. Newest first.

## 1.0.0

- First public release: Apache, PHP 7.3 / 8.1–8.5, MySQL 8.4 / 9.7, Redis, phpMyAdmin, on-demand Elasticsearch.
- Vhosts with automatic /etc/hosts management (privileged helper).
- Service updates via the signed binaries manifest; app updates via Sparkle.
